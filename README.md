# MITM-Simulation
MITM simulation utilizing the AESGCM class from Python’s cryptography library.

## Requirements
Before running this program, make sure you have Python 3 installed and the cryptography library installed.

## Install dependency
```bash
pip install cryptography
